---
name: context-audit
description: "审查 Agent 上下文中的重复、冲突、过时内容和职责错位。"
---

# 上下文审查

审查 Agent 实际会使用的上下文，减少冲突、重复和不必要的长期上下文，并让长期有效的规则由合适的位置维护。

## 流程

1. 确认可访问的审查范围，包括系统规则、`AGENTS.md`、项目规则、Skills、参考资料、Memory、工具说明和示例。区分已确认加载、可能加载和不可见内容；文件存在不代表已加载，不对不可见内容作完整性结论。
2. 找出重复规则、互相冲突的要求、过时信息、适用范围错误，以及长期规则与临时任务信息混放等问题。
3. 去重前核对指令优先级、适用范围和加载条件，不能只因文字相似而删除。判断内容应该长期保留、按需加载、移入 `references/`、由脚本或工具强制执行，还是直接删除；迁移后确认所需内容仍能被发现和加载。
4. 对每项建议说明当前问题、实际影响、建议位置和迁移风险；已有明确维护者的规则不要在其他位置重复定义。
5. 优先做最小调整，避免为了整理结构而重构整套提示词体系。

## 边界

这里只审查上下文组织和指令职责，不评价业务代码质量，也不负责重新设计完整的 Agent 架构。涉及行为改变时必须说明影响范围，不能只以减少上下文占用为理由修改内容。
