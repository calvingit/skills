interface:
  display_name: "To Spec"
  short_description: "Create or amend a requirement SPEC, and judge whether HLD or an execution graph is next"
  default_prompt: "Use $to-spec to create or amend SPEC.md from requirement authority, keep requirement constraints distinct from high-level design, and judge whether downstream work needs an HLD or an execution graph."

policy:
  allow_implicit_invocation: false
