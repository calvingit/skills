interface:
  display_name: "Grilling"
  short_description: "Settle decisions in the project task directory"
  default_prompt: "Use $grilling. Read Profile.task_contract; use its task directory, or create one under its root for all session docs. Without task_contract, suggest .grilling/grilling-<UTC timestamp>/ and remind once about project-setup."

policy:
  allow_implicit_invocation: false
