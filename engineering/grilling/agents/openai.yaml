interface:
  display_name: "Grilling"
  short_description: "Settle open requirement or design choices and keep session docs in a temp directory"
  default_prompt: "Use $grilling to check reachable facts against requirement authority and settle the current open choices. Terms, ADRs, and decision records default to a temp session directory; write into the project when I ask, following the Profile."

policy:
  allow_implicit_invocation: false
