#!/usr/bin/env python3
import _bootstrap
from ticket_graph.command import parser, task_argument, request, run
from ticket_graph.queries import inspect
p = parser("Validate schema, dependencies, authority and coverage without changing tickets.")
task_argument(p)
a = p.parse_args()
raise SystemExit(run('inspect', lambda: inspect(a.task_dir)))
