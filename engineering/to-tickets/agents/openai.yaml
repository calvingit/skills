interface:
  display_name: "To Tickets"
  short_description: "将已确认 SPEC 拆成带真实 blocking edges 的本地 vertical-slice tickets"
  default_prompt: "使用 $to-tickets 将已确认的 SPEC.md 拆成有真实依赖的可执行 tickets。"

policy:
  allow_implicit_invocation: false
