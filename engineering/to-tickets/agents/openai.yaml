interface:
  display_name: "To Tickets"
  short_description: "Derive or sync an execution graph from SPEC and optional HLD"
  default_prompt: "Use $to-tickets to derive tickets from the confirmed SPEC.md and optional HLD.md, or to sync the affected execution graph after an upstream amendment."

policy:
  allow_implicit_invocation: false
