interface:
  display_name: "Improve Codebase Architecture"
  short_description: "Find deepening candidates and drive architecture design from a visual report"
  default_prompt: "Use $improve-codebase-architecture to scan the current range for deepening candidates and produce a visual report."

policy:
  allow_implicit_invocation: false
