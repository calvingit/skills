interface:
  display_name: "Project Setup"
  short_description: "Detect and persist workflow, requirement-authority, domain-doc, and collaboration entries"
  default_prompt: "Use $project-setup to detect this project and configure an Engineering Skills Profile that includes the requirement-authority entry."

policy:
  allow_implicit_invocation: false
