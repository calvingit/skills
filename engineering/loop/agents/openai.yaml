interface:
  display_name: "Loop"
  short_description: "调度多-ticket frontier 并聚合 landed evidence"
  default_prompt: "使用 $loop 计算当前 ready frontier，调度 implement 执行 tickets，并依据 landed evidence 推进 execution graph。"

policy:
  allow_implicit_invocation: false
