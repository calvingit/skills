interface:
  display_name: "Verify"
  short_description: "Verify ticket Acceptance Criteria in an independent read-only context"
  default_prompt: "Use $verify to check each Acceptance Criterion against the given implementation snapshot and return evidence that can be rechecked."

policy:
  allow_implicit_invocation: false
