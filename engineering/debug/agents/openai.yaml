interface:
  display_name: "Debug"
  short_description: "Reproduce and diagnose a confirmed bug; apply a minimal fix and regression only when authorised"
  default_prompt: "Use $debug to reproduce and diagnose the current problem. If I asked for a fix, then apply the smallest fix and regression."

policy:
  allow_implicit_invocation: true
