---
name: verify
description: "验证任务是否真正完成，并用可复现的执行证据确认需求、构建、测试和关键行为；不承担代码质量审查。"
---

# 完成情况验证

确认任务是否达到完成条件，以及现有证据是否充分。重点回答“是否按要求工作、证据是什么”，不评价代码或设计质量。

## 与 `code-review` 的边界

`verify` 只验证可观察结果和完成条件，不检查代码规范、设计质量、代码异味、抽象合理性或实现风格；这些属于 `code-review`。

实现即使存在质量或设计问题，只要满足明确的完成条件且证据有效，`verify` 仍可判定通过。反过来，即使代码质量良好，只要关键行为尚未验证或验证失败，也不能判定通过。

## 流程

1. 从用户要求、SPEC、ticket 或调用方提供的信息中提取可验证的完成条件。没有明确条件时，只验证能够客观确认的部分，不自行扩展需求。
2. 根据变更范围和风险选择最小必要验证：针对性测试、lint、类型检查、构建、运行时检查、接口调用、静态搜索、差异检查，或项目已有的其他验证方式。
3. 优先复用仓库现有脚本、Make target、CI 命令和测试入口，不为了验证而新增生产代码或测试专用接口。
4. 记录实际执行的命令、关键输出和失败项。无法执行的检查明确标为未验证，不能用代码阅读代替运行证据。
5. 对照完成条件逐项给出 `verified`、`failed` 或 `not_verified`，并附上证据。

## 边界

默认不修改产品代码来让验证通过。发现失败时报告现象和证据；需要定位原因时交给 `debug`，需要评价代码质量或需求、HLD 的实现质量时交给 `code-review`。
