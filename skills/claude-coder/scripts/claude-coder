#!/usr/bin/env bash
set -euo pipefail

TASK="${*:-}"

if [ -z "$TASK" ]; then
  echo "用法：claude-coder <编码任务描述>" >&2
  exit 2
fi

claude --dangerously-skip-permissions "以最小、安全、可验证的改动完成以下编码任务。

## 执行规则
- 只检查与任务相关的文件
- 做最小可行改动，不做无关重构或格式调整
- 保持现有代码风格、命名规范和架构设计
- 除非任务明确要求，不得删除文件
- 未实际运行并通过验证命令，不得声称验证通过

## 验证
编辑完成后，运行最相关的检查：
- 单元测试、lint、类型检查或构建
- 若无适用检查，说明原因

## 输出格式（仅输出以下内容）

\`\`\`md
## 结果
<改了什么、为什么改>

## 变更文件
- \`<路径>\`：<原因>

## 验证
- \`<命令>\`：<通过 / 失败 / 未执行>
- 备注：<简短说明>

## 风险
<遗留风险，或"无">
\`\`\`

## 任务
$TASK"
